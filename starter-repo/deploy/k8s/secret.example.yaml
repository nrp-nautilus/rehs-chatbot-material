# ============================================================================
# TEMPLATE ONLY — DO NOT COMMIT THE REAL ONE.
#
# This file shows the SHAPE of the secret. The value below is FAKE. Putting a
# real NRP LLM token in a committed file leaks it to the whole internet — if you
# ever do, rotate it immediately at https://nrp.ai/llmtoken.
#
# Preferred: create the secret imperatively (nothing to commit):
#
#   kubectl create secret generic chatbot-llm-token \
#     --from-literal=NRP_LLM_TOKEN=sk-yourtoken \
#     -n <<NAMESPACE>>
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: chatbot-llm-token
  namespace: <<NAMESPACE>>
type: Opaque
stringData:
  # FAKE placeholder — replace at apply time, NEVER commit the real value.
  NRP_LLM_TOKEN: "sk-REPLACE-ME-DO-NOT-COMMIT-THE-REAL-ONE"
