# Pinned dependencies. These versions match the curriculum (Week 5).
# Bump deliberately — a version drift can change behavior.
openai==1.55.0
httpx==0.27.2          # openai 1.55 breaks on httpx>=0.28 (removed 'proxies' arg) — keep pinned
python-dotenv==1.0.1
streamlit==1.39.0
chromadb==0.5.20
beautifulsoup4==4.12.3
requests==2.32.3
matplotlib==3.9.2      # Session 2 eval charts (Figures 11 & 12)
